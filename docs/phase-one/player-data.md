# Player data

Typed in `src/lib/types/player.ts` and validated with Zod on load.

Persisted fields include explorer profile, inventory, daily claims, wheel spins, quest states, arcade scores, decorations, worn items, journal unlocks, and world flags.

Reset from Settings → “Reset local profile (dev)”.
