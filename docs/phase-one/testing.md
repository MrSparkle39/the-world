# Testing

```bash
npm test
npm run typecheck
npm run lint
npm run build
```

Critical unit coverage lives in `tests/game-logic.test.ts` (dailies, wheel, purchases, inventory, arcade caps, age dialogue, decorations, moon flag).
