# Next.js App Router on Netlify (OpenNext runtime).
# Do not set publish to "out", "legacy", or ".next" in the Netlify UI — leave blank.

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
