# Next.js App Router on Netlify (OpenNext runtime).
# Leave Publish directory BLANK in the Netlify UI — do not set ".next" or ".next-dev".

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
